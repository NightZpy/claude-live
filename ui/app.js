const I18N = {
  es: {
    sessions: "Sesiones",
    active: "activas",
    archived: "Archivadas",
    search_placeholder: "buscar sesiones…",
    waiting_you: "esperándote",
    idle: "idle",
    running: "corriendo",
    no_sessions: "sin sesiones",
    files: "archivos",
    events: "eventos",
    close: "cerrar",
    started: "iniciada",
    ended: "terminó",
    sys_task: "tarea background",
    sys_reminder: "sistema",
    sys_command: "comando",
    kind_worker: "worker",
    workers_group: "workers (subagentes)",
    reason_exit: "terminó",
    reason_other: "terminó",
    reason_clear: "clear",
    reason_process_died: "proceso murió",
    reason_stale: "inactiva 24h+",
    filter_all: "todas",
    filter_today: "hoy",
    filter_show_workers: "mostrar workers",
    filter_hide_workers: "ocultar workers",
    outside_project: "fuera del proyecto",
    scratchpad: "scratchpad",
    preview_too_large: "archivo muy grande",
    preview_missing: "archivo no encontrado",
    preview_binary: "archivo binario",
    preview_error: "error de vista previa",
    tasks_section: "Tareas",
    task_open: "abierta",
    task_in_progress: "en progreso",
    task_done: "hecha",
    task_blocked: "bloqueada",
    task_delegated: "delegada",
    task_blocked_by: "bloqueada por",
    task_delegated_to: "delegada a",
    task_opened_at: "abierta",
    task_closed_at: "cerrada",
    match_file: "archivo",
    match_event: "evento",
    match_session: "sesión",
    opened_ago: "abierta",
    daily_title: "Hoy",
    daily_yesterday: "Ayer",
    daily_today: "Hoy",
    daily_blockers: "Bloqueos",
    daily_regenerate: "Regenerar",
    daily_empty: "—",
    slack_section: "Slack",
    slack_unresolved: "sin resolver",
    slack_reasks: "re-preguntas",
    slack_unlinked: "sin ligar",
    slack_mark_resolved: "✓ resolver",
    signals_section: "Señales",
    signal_alert: "alerta",
    signal_deploy: "deploy",
    resume_copy: "Copiar prompt de recuperación",
    resume_copy_id: "copiar id",
    resume_copied: "copiado",
    resume_failed: "error al copiar",
    links_repos: "Repos & PRs",
    links_linear: "Linear",
    links_artifacts: "Artefactos",
    settings_title: "Configuración",
    settings_language: "Idioma",
    settings_notify: "Notificar cuando Claude espera tu input",
    settings_summaries_auto: "Resúmenes automáticos",
    settings_daily_auto: "Daily automático",
    settings_alerts: "Canales de alertas (separados por coma)",
    settings_deploys: "Canales de deploys (separados por coma)",
    settings_instances: "Instancias activas",
    settings_slack_token: "Slack token",
    settings_linear_token: "Linear token",
    summary_generate: "generar resumen",
    settings_slack_token_set: "configurado ····",
    settings_save: "Guardar",
    settings_saved: "guardado",
    settings_clear: "Borrar",
    fg_status: "estado",
    fg_instance: "instancia",
    fg_time: "actividad",
    daily_auto: "auto",
    daily_copy_slack: "copiar para Slack",
    daily_lang_es: "ES",
    daily_lang_en: "EN",
    day_yesterday_desc: "lo que avancé",
    day_today_desc: "lo que sigue",
    day_blockers_desc: "lo que me traba",
  },
  en: {
    sessions: "Sessions",
    active: "active",
    archived: "Archived",
    search_placeholder: "search sessions…",
    waiting_you: "waiting for you",
    idle: "idle",
    running: "running",
    no_sessions: "no sessions",
    files: "files",
    events: "events",
    close: "close",
    started: "started",
    ended: "ended",
    sys_task: "background task",
    sys_reminder: "system",
    sys_command: "command",
    kind_worker: "worker",
    workers_group: "workers (subagents)",
    reason_exit: "finished",
    reason_other: "finished",
    reason_clear: "clear",
    reason_process_died: "process died",
    reason_stale: "stale 24h+",
    filter_all: "all",
    filter_today: "today",
    filter_show_workers: "show workers",
    filter_hide_workers: "hide workers",
    outside_project: "outside project",
    scratchpad: "scratchpad",
    preview_too_large: "file too large",
    preview_missing: "file not found",
    preview_binary: "binary file",
    preview_error: "preview error",
    tasks_section: "Tasks",
    task_open: "open",
    task_in_progress: "in progress",
    task_done: "done",
    task_blocked: "blocked",
    task_delegated: "delegated",
    task_blocked_by: "blocked by",
    task_delegated_to: "delegated to",
    task_opened_at: "opened",
    task_closed_at: "closed",
    match_file: "file",
    match_event: "event",
    match_session: "session",
    opened_ago: "opened",
    daily_title: "Today",
    daily_yesterday: "Yesterday",
    daily_today: "Today",
    daily_blockers: "Blockers",
    daily_regenerate: "Regenerate",
    daily_empty: "—",
    slack_section: "Slack",
    slack_unresolved: "unresolved",
    slack_reasks: "re-asks",
    slack_unlinked: "unlinked",
    slack_mark_resolved: "✓ resolve",
    signals_section: "Signals",
    signal_alert: "alert",
    signal_deploy: "deploy",
    resume_copy: "Copy recovery prompt",
    resume_copy_id: "copy id",
    resume_copied: "copied",
    resume_failed: "copy failed",
    links_repos: "Repos & PRs",
    links_linear: "Linear",
    links_artifacts: "Artifacts",
    settings_title: "Settings",
    settings_language: "Language",
    settings_notify: "Notify when Claude is waiting for input",
    settings_summaries_auto: "Auto summaries",
    settings_daily_auto: "Auto daily",
    settings_alerts: "Alert channels (comma-separated)",
    settings_deploys: "Deploy channels (comma-separated)",
    settings_instances: "Active instances",
    settings_slack_token: "Slack token",
    settings_linear_token: "Linear token",
    summary_generate: "generate summary",
    settings_slack_token_set: "set ····",
    settings_save: "Save",
    settings_saved: "saved",
    settings_clear: "Clear",
    fg_status: "status",
    fg_instance: "instance",
    fg_time: "activity",
    daily_auto: "auto",
    daily_copy_slack: "copy for Slack",
    daily_lang_es: "ES",
    daily_lang_en: "EN",
    day_yesterday_desc: "what I did",
    day_today_desc: "what's next",
    day_blockers_desc: "what's blocking",
  },
  pt: {
    sessions: "Sessões",
    active: "ativas",
    archived: "Arquivadas",
    search_placeholder: "buscar sessões…",
    waiting_you: "esperando você",
    idle: "idle",
    running: "rodando",
    no_sessions: "sem sessões",
    files: "arquivos",
    events: "eventos",
    close: "fechar",
    started: "iniciada",
    ended: "terminou",
    sys_task: "tarefa background",
    sys_reminder: "sistema",
    sys_command: "comando",
    kind_worker: "worker",
    workers_group: "workers (subagentes)",
    reason_exit: "terminou",
    reason_other: "terminou",
    reason_clear: "clear",
    reason_process_died: "processo morreu",
    reason_stale: "inativa 24h+",
    filter_all: "todas",
    filter_today: "hoje",
    filter_show_workers: "mostrar workers",
    filter_hide_workers: "ocultar workers",
    outside_project: "fora do projeto",
    scratchpad: "scratchpad",
    preview_too_large: "arquivo muito grande",
    preview_missing: "arquivo não encontrado",
    preview_binary: "arquivo binário",
    preview_error: "erro de pré-visualização",
    tasks_section: "Tarefas",
    task_open: "aberta",
    task_in_progress: "em progresso",
    task_done: "feita",
    task_blocked: "bloqueada",
    task_delegated: "delegada",
    task_blocked_by: "bloqueada por",
    task_delegated_to: "delegada a",
    task_opened_at: "aberta",
    task_closed_at: "fechada",
    match_file: "arquivo",
    match_event: "evento",
    match_session: "sessão",
    opened_ago: "aberta",
    daily_title: "Hoje",
    daily_yesterday: "Ontem",
    daily_today: "Hoje",
    daily_blockers: "Bloqueios",
    daily_regenerate: "Regenerar",
    daily_empty: "—",
    slack_section: "Slack",
    slack_unresolved: "sem resolver",
    slack_reasks: "re-perguntas",
    slack_unlinked: "sem vincular",
    slack_mark_resolved: "✓ resolver",
    signals_section: "Sinais",
    signal_alert: "alerta",
    signal_deploy: "deploy",
    resume_copy: "Copiar prompt de recuperação",
    resume_copy_id: "copiar id",
    resume_copied: "copiado",
    resume_failed: "erro ao copiar",
    links_repos: "Repos & PRs",
    links_linear: "Linear",
    links_artifacts: "Artefatos",
    settings_title: "Configurações",
    settings_language: "Idioma",
    settings_notify: "Notificar quando Claude aguarda input",
    settings_summaries_auto: "Resumos automáticos",
    settings_daily_auto: "Daily automático",
    settings_alerts: "Canais de alertas (separados por vírgula)",
    settings_deploys: "Canais de deploys (separados por vírgula)",
    settings_instances: "Instâncias ativas",
    settings_slack_token: "Slack token",
    settings_linear_token: "Linear token",
    summary_generate: "gerar resumo",
    settings_slack_token_set: "configurado ····",
    settings_save: "Salvar",
    settings_saved: "salvo",
    settings_clear: "Limpar",
    fg_status: "estado",
    fg_instance: "instância",
    fg_time: "atividade",
    daily_auto: "auto",
    daily_copy_slack: "copiar para Slack",
    daily_lang_es: "ES",
    daily_lang_en: "EN",
    day_yesterday_desc: "o que avancei",
    day_today_desc: "o que segue",
    day_blockers_desc: "o que trava",
  },
};

let lang = "es";
let t = I18N.es;
let openId = null;
let dailyLang = null; // 'es' or 'en', set in renderDaily

let inSearchMode = false;
let searchDebounce = null;

let filterStatus = 'all';    // 'all' | 'waiting_input' | 'running' | 'idle'
let filterInstance = 'all';  // 'all' | <instance string>
let filterTime = 'all';      // 'all' | '5m' | '30m' | '1h' | 'today'
let filterWorkers = true;    // true = show workers (grouped), false = hide entirely

function loadFilters() {
  filterStatus = localStorage.getItem('cl-filter-status') || 'all';
  filterInstance = localStorage.getItem('cl-filter-instance') || 'all';
  filterTime = localStorage.getItem('cl-filter-time') || 'all';
  filterWorkers = localStorage.getItem('cl-filter-workers') !== 'false';
}

function saveFilters() {
  localStorage.setItem('cl-filter-status', filterStatus);
  localStorage.setItem('cl-filter-instance', filterInstance);
  localStorage.setItem('cl-filter-time', filterTime);
  localStorage.setItem('cl-filter-workers', String(filterWorkers));
}

function fmtDur(ms) {
  if (ms < 0) ms = 0;
  const s = Math.floor(ms / 1000);
  const m = Math.floor(s / 60);
  const h = Math.floor(m / 60);
  const d = Math.floor(h / 24);
  if (d > 0) return d + "d " + (h % 24) + "h";
  if (h > 0) return h + "h " + (m % 60) + "m";
  if (m > 0) return m + "m";
  return s + "s";
}

function rel(ts) {
  if (!ts) return "";
  return fmtDur(Date.now() - ts);
}

function esc(s) {
  return String(s == null ? "" : s)
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    .replace(/"/g, "&quot;");
}

function mdToSafeHtml(escapedText) {
  var lines = escapedText.split('\n');
  var out = [];
  var inCode = false;
  var codeLines = [];
  var listBuf = null;
  var quoteBuf = [];

  function flushList() {
    if (!listBuf) return;
    out.push('<' + listBuf.tag + '>' + listBuf.items.map(function(i) { return '<li>' + i + '</li>'; }).join('') + '</' + listBuf.tag + '>');
    listBuf = null;
  }

  function flushQuote() {
    if (!quoteBuf.length) return;
    out.push('<blockquote>' + quoteBuf.join('<br>') + '</blockquote>');
    quoteBuf = [];
  }

  function inline(s) {
    s = s.replace(/`([^`]+)`/g, '<code>$1</code>');
    s = s.replace(/\[([^\]]+)\]\(([^)]+)\)/g, function(_, text, url) {
      if (url.startsWith('http://') || url.startsWith('https://')) {
        var safeUrl = url.replace(/&quot;/g, '%22').replace(/\s.*$/, '');
        return '<a href="' + safeUrl + '" target="_blank" rel="noopener">' + text + '</a>';
      }
      return '[' + text + '](' + url + ')';
    });
    s = s.replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>');
    s = s.replace(/__(.+?)__/g, '<strong>$1</strong>');
    s = s.replace(/(?<!\*)\*(?!\*)(.+?)(?<!\*)\*(?!\*)/g, '<em>$1</em>');
    s = s.replace(/(?<!_)_(?!_)(.+?)(?<!_)_(?!_)/g, '<em>$1</em>');
    return s;
  }

  for (var i = 0; i < lines.length; i++) {
    var line = lines[i];
    if (line.trim() === '```') {
      if (inCode) {
        out.push('<pre><code>' + codeLines.join('\n') + '</code></pre>');
        codeLines = [];
        inCode = false;
      } else {
        flushList(); flushQuote();
        inCode = true;
      }
      continue;
    }
    if (inCode) { codeLines.push(line); continue; }

    var hm = line.match(/^(#{1,6}) (.+)$/);
    if (hm) {
      flushList(); flushQuote();
      out.push('<h' + hm[1].length + '>' + inline(hm[2]) + '</h' + hm[1].length + '>');
      continue;
    }

    if (line.startsWith('&gt; ')) {
      flushList();
      quoteBuf.push(inline(line.slice(5)));
      continue;
    }

    if (line.startsWith('- ') || line.startsWith('* ')) {
      flushQuote();
      var item = inline(line.slice(2));
      if (listBuf && listBuf.tag === 'ul') listBuf.items.push(item);
      else { flushList(); listBuf = { tag: 'ul', items: [item] }; }
      continue;
    }

    var om = line.match(/^\d+\. (.+)$/);
    if (om) {
      flushQuote();
      var olItem = inline(om[1]);
      if (listBuf && listBuf.tag === 'ol') listBuf.items.push(olItem);
      else { flushList(); listBuf = { tag: 'ol', items: [olItem] }; }
      continue;
    }

    flushList(); flushQuote();
    if (line.trim() === '') { out.push('<br>'); continue; }
    out.push('<p>' + inline(line) + '</p>');
  }

  flushList(); flushQuote();
  if (inCode) out.push('<pre><code>' + codeLines.join('\n') + '</code></pre>');

  return out.join('');
}

var EVENT_META = {
  prompt:        { icon: '›', color: '#58a6ff' },
  waiting:       { icon: '⏸', color: '#d29922' },
  stop:          { icon: '■', color: '#484f58' },
  session_start: { icon: '●', color: '#3fb950' },
  session_end:   { icon: '○', color: '#6e7681' },
};

function eventMeta(kind) {
  return EVENT_META[kind] || { icon: '·', color: '#484f58' };
}

function buildEventsFilterRow(presentKinds, activeEvKinds) {
  if (!presentKinds.length) return '';
  var isAll = activeEvKinds.size === 0 || presentKinds.every(function(k) { return activeEvKinds.has(k); });
  var chips = '';
  chips += '<span class="ev-chip' + (isAll ? ' active' : '') + '" data-kind="all">todas</span>';
  presentKinds.forEach(function(kind) {
    var meta = eventMeta(kind);
    var isActive = isAll || activeEvKinds.has(kind);
    chips += '<span class="ev-chip' + (isActive ? ' active' : '') + '" data-kind="' + esc(kind) + '">' +
      '<span style="color:' + meta.color + '">' + meta.icon + '</span> ' + esc(kind) +
    '</span>';
  });
  if (presentKinds.indexOf('prompt') !== -1) {
    var soloActive = activeEvKinds.size === 1 && activeEvKinds.has('prompt');
    chips += '<span class="ev-chip ev-chip-solo' + (soloActive ? ' active' : '') + '" data-kind="__solo_prompts__">solo prompts</span>';
  }
  return '<div class="ev-filter-row">' + chips + '</div>';
}

function decodePrompt(text) {
  const trimmed = (text || "").trimStart();
  if (trimmed.startsWith("<task-notification>")) {
    const summary = (/<summary>([\s\S]*?)<\/summary>/.exec(trimmed) || [])[1] || "";
    const status  = (/<status>([\s\S]*?)<\/status>/.exec(trimmed) || [])[1] || "";
    const taskId  = (/<task-id>([\s\S]*?)<\/task-id>/.exec(trimmed) || [])[1] || "";
    let decoded = summary.trim();
    if (status.trim()) decoded += " · " + status.trim();
    if (!decoded) decoded = taskId.trim();
    return { kind: "system", label: "sys_task", text: decoded };
  }
  if (trimmed.startsWith("<system-reminder>")) {
    const inner = trimmed.replace(/<[^>]+>/g, " ").replace(/\s+/g, " ").trim();
    return { kind: "system", label: "sys_reminder", text: inner };
  }
  if (trimmed.startsWith("<command-name>")) {
    const m = /<command-name>([\s\S]*?)<\/command-name>/.exec(trimmed);
    return { kind: "system", label: "sys_command", text: m ? m[1].trim() : "" };
  }
  if (trimmed.startsWith("<local-command-caveat>")) {
    const inner = trimmed.replace(/<[^>]+>/g, " ").replace(/\s+/g, " ").trim();
    return { kind: "system", label: "sys_command", text: inner };
  }
  return { kind: "user" };
}

function sysChip(label, decoded) {
  return (
    '<span class="badge-sys">' + esc(t[label] || label) + "</span>" +
    ' <em class="dim">' + esc(decoded) + "</em>"
  );
}

function basename(p) {
  if (!p) return "";
  return p.replace(/\/$/, "").split("/").pop() || p;
}

function relPath(filePath, cwd) {
  if (!cwd) return null;
  var cwdSlash = cwd.endsWith('/') ? cwd : cwd + '/';
  if (filePath.startsWith(cwdSlash)) return filePath.slice(cwdSlash.length);
  return null;
}

function fileGroup(filePath, cwd) {
  if (/\/tmp\/claude-[^/]+(?:\/.+)?\/scratchpad\//.test(filePath)) {
    return { dir: '__scratchpad__', label: t.scratchpad || 'scratchpad' };
  }
  var rel2 = relPath(filePath, cwd);
  if (rel2 === null) {
    return { dir: '__outside__', label: t.outside_project || 'outside project' };
  }
  var parts = rel2.split('/');
  var dir = parts.length > 1 ? parts.slice(0, -1).join('/') : '.';
  return { dir: dir, label: dir };
}

function dotClass(status) {
  if (status === "running") return "d-run";
  if (status === "waiting_input") return "d-wait";
  if (status === "idle") return "d-idle";
  return "d-arch";
}

function fmtTime(ts) {
  if (!ts) return "";
  return new Date(ts).toLocaleTimeString([], { hour: "2-digit", minute: "2-digit" });
}

function fmtAbsDateTime(ts) {
  if (!ts) return "";
  return new Date(ts).toLocaleString([], { month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit' });
}

function archiveReasonLabel(reason) {
  if (!reason) return '';
  if (reason === 'exit' || reason === 'other') return t.reason_exit || t.reason_other || reason;
  if (reason === 'clear') return t.reason_clear || reason;
  if (reason === 'process_died') return t.reason_process_died || reason;
  if (reason === 'stale') return t.reason_stale || reason;
  return reason;
}

function buildRow(s) {
  const displayName = s.name || basename(s.cwd) || s.id;
  const dc = dotClass(s.status);

  let right = "";
  if (s.status === "waiting_input" && s.waiting_since) {
    const wm = Math.floor((Date.now() - s.waiting_since) / 60000);
    right = '<span class="amber">⏸ ' + wm + "m " + esc(t.waiting_you) + "</span>";
  } else if (s.status === "archived") {
    const dur = s.ended_at && s.started_at ? fmtDur(s.ended_at - s.started_at) : "";
    const reason = s.archived_reason ? esc(archiveReasonLabel(s.archived_reason)) : "";
    right = '<span class="dim">' + (reason ? reason + " · " : "") + esc(dur) + "</span>";
  } else {
    right = '<span class="dim">' + rel(s.last_activity) + "</span>";
  }

  let topicHtml = "";
  if (s.summary) {
    topicHtml = '<div class="topic">' + esc(s.summary.slice(0, 90)) + "</div>";
  } else if (s.last_prompt) {
    const dec = decodePrompt(s.last_prompt);
    const inner = dec.kind === "system"
      ? sysChip(dec.label, dec.text.slice(0, 90))
      : esc(s.last_prompt.slice(0, 90));
    topicHtml = '<div class="topic">' + inner + "</div>";
  }

  const kindBadge = s.kind === 'worker'
    ? '<span class="badge-kind">' + esc(t.kind_worker || 'worker') + '</span>'
    : '';

  return (
    '<div class="row srow" data-id="' + esc(s.id) + '" tabindex="0">' +
      '<span class="dot ' + dc + '"></span>' +
      '<div class="srow-body">' +
        '<div class="srow-top">' +
          '<span class="name">' + esc(displayName) + "</span>" +
          '<span class="badge-instance tag">' + esc(s.instance || "") + "</span>" +
          kindBadge +
        "</div>" +
        topicHtml +
      "</div>" +
      '<div class="srow-right">' + right + "</div>" +
    "</div>"
  );
}

function buildWorkersGroup(workers) {
  if (!workers.length) return '';
  const label = t.workers_group || 'workers';
  return '<details class="workers-group"><summary>' + esc(label) + ' (' + workers.length + ')</summary>'
    + workers.map(buildRow).join('')
    + '</details>';
}

function taskStatusClass(status) {
  if (status === 'open') return 'amber';
  if (status === 'in_progress') return 'blue';
  if (status === 'done') return 'dim';
  if (status === 'blocked') return 'red';
  if (status === 'delegated') return 'violet';
  return 'muted';
}

function buildTasksBody(tasks) {
  if (!tasks || tasks.length === 0) return '<div class="dim">—</div>';
  return tasks.map(function(task, idx) {
    var cls = taskStatusClass(task.status);
    var label = t['task_' + task.status] || task.status;
    var age = task.opened_at ? rel(task.opened_at) : '';

    var rowHtml =
      '<div class="frow task-row" data-task-idx="' + idx + '" style="cursor:pointer">' +
        '<span class="' + esc(cls) + '">' + esc(label) + '</span>' +
        '<span style="flex:1;padding:0 8px;overflow:hidden;text-overflow:ellipsis">' +
          '<span class="task-caret dim" style="margin-right:4px;font-size:10px">▶</span>' +
          esc(task.title) +
        '</span>' +
        '<span class="dim">' + esc(t.opened_ago || 'abierta') + ' ' + esc(age) + '</span>' +
      '</div>';

    // Build expanded detail lines
    var lines = [];

    if (task.context) {
      lines.push('<span class="dim" style="font-style:italic">' + esc(task.context) + '</span>');
    }

    if (task.blocked_on) {
      var blockLabel = task.status === 'delegated'
        ? (t.task_delegated_to || 'delegada a')
        : (t.task_blocked_by || 'bloqueada por');
      lines.push(
        '<span class="dim">' + esc(blockLabel) + ': </span>' +
        '<span>' + esc(task.blocked_on) + '</span>'
      );
    }

    if (task.opened_at) {
      lines.push(
        '<span class="dim">' +
          esc(t.task_opened_at || 'abierta') + ': ' +
          esc(rel(task.opened_at)) + ' (' + esc(fmtAbsDateTime(task.opened_at)) + ')' +
        '</span>'
      );
    }

    if (task.closed_at && task.status === 'done') {
      lines.push(
        '<span class="dim">' +
          esc(t.task_closed_at || 'cerrada') + ': ' +
          esc(rel(task.closed_at)) + ' (' + esc(fmtAbsDateTime(task.closed_at)) + ')' +
        '</span>'
      );
    }

    var expandedHtml =
      '<div class="task-expanded" data-task-idx="' + idx + '" hidden ' +
        'style="padding:4px 8px 6px 32px;font-size:12px;line-height:1.8;border-bottom:1px solid #21262d">' +
        (lines.length ? lines.join('<br>') : '<span class="dim">—</span>') +
      '</div>';

    return rowHtml + expandedHtml;
  }).join('');
}

function buildSearchRow(r) {
  var rowHtml = buildRow(r.session);
  var chipKey = 'match_' + r.matched_kind;
  var chip = '<span class="badge-sys">' + esc(t[chipKey] || r.matched_kind) + '</span> ';
  var snippetEscaped = esc(r.snippet || '').replace(/\[([^\]]*)\]/g, function(_, w) {
    return '<mark>' + w + '</mark>';
  });
  var snippetHtml = r.snippet
    ? '<div class="topic dim">' + chip + snippetEscaped + '</div>'
    : '';
  return rowHtml.replace('</div><div class="srow-right">', snippetHtml + '</div><div class="srow-right">');
}

function renderSearchResults(results) {
  inSearchMode = true;
  document.getElementById("lbl-active").textContent = results.length + ' resultados';
  var archLbl = document.getElementById("lbl-archived");
  var archEl = document.getElementById("archived");
  if (archLbl) archLbl.style.display = 'none';
  if (archEl) archEl.style.display = 'none';
  var sessEl = document.getElementById("sessions");
  if (results.length === 0) {
    sessEl.innerHTML = '<div class="row muted">' + esc(t.no_sessions) + '</div>';
  } else {
    sessEl.innerHTML = results.map(buildSearchRow).join('');
  }
  attachRowHandlers();
}

function exitSearchMode() {
  if (!inSearchMode) return;
  inSearchMode = false;
  var archLbl = document.getElementById("lbl-archived");
  var archEl = document.getElementById("archived");
  if (archLbl) archLbl.style.display = '';
  if (archEl) archEl.style.display = '';
}

function attachRowHandlers() {
  document.querySelectorAll(".srow").forEach(function (row) {
    row.addEventListener("click", function () { openDetail(row.dataset.id); });
    row.addEventListener("keydown", function (e) {
      if (e.key === "Enter" || e.key === " ") openDetail(row.dataset.id);
    });
  });
}

function renderFilters(allSessions) {
  const bar = document.getElementById('filters');
  if (!bar) return;

  const statuses = [
    { key: 'all', label: t.filter_all || 'todas' },
    { key: 'waiting_input', label: t.waiting_you },
    { key: 'running', label: t.running },
    { key: 'idle', label: t.idle },
  ];
  const statusChips = statuses.map(function(s) {
    const active = filterStatus === s.key ? ' active' : '';
    return '<span class="chip seg-opt' + active + '" data-filter="status" data-val="' + esc(s.key) + '">' + esc(s.label) + '</span>';
  }).join('');

  const instances = ['all'].concat(
    [...new Set(allSessions.map(function(s) { return s.instance; }).filter(Boolean))].sort()
  );
  const instanceChips = instances.map(function(inst) {
    const active = filterInstance === inst ? ' active' : '';
    const label = inst === 'all' ? (t.filter_all || 'todas') : inst;
    return '<span class="chip seg-opt' + active + '" data-filter="instance" data-val="' + esc(inst) + '">' + esc(label) + '</span>';
  }).join('');

  const times = [
    { key: 'all', label: t.filter_all || 'todas' },
    { key: '5m', label: '5m' },
    { key: '30m', label: '30m' },
    { key: '1h', label: '1h' },
    { key: 'today', label: t.filter_today || 'hoy' },
  ];
  const timeChips = times.map(function(tm) {
    const active = filterTime === tm.key ? ' active' : '';
    return '<span class="chip seg-opt' + active + '" data-filter="time" data-val="' + esc(tm.key) + '">' + esc(tm.label) + '</span>';
  }).join('');

  const wLabel = filterWorkers ? (t.filter_hide_workers || 'ocultar workers') : (t.filter_show_workers || 'mostrar workers');
  const wPrefix = filterWorkers ? '👁️ ' : '';
  const workersChip = '<span class="chip chip-toggle' + (filterWorkers ? ' active' : '') + '" data-filter="workers">' + wPrefix + esc(wLabel) + '</span>';

  bar.innerHTML =
    '<div class="filter-row">' +
      '<span class="filter-glabel">' + esc(t.fg_status || 'estado') + '</span>' +
      '<div class="seg">' + statusChips + '</div>' +
    '</div>' +
    '<div class="filter-row">' +
      '<span class="filter-glabel">' + esc(t.fg_instance || 'instancia') + '</span>' +
      '<div class="seg">' + instanceChips + '</div>' +
    '</div>' +
    '<div class="filter-row">' +
      '<span class="filter-glabel">' + esc(t.fg_time || 'actividad') + '</span>' +
      '<div class="filter-time-row">' +
        '<div class="seg">' + timeChips + '</div>' +
        workersChip +
      '</div>' +
    '</div>';

  bar.querySelectorAll('.chip').forEach(function(chip) {
    chip.addEventListener('click', function() {
      const filter = chip.dataset.filter;
      const val = chip.dataset.val;
      if (filter === 'status') filterStatus = val;
      else if (filter === 'instance') filterInstance = val;
      else if (filter === 'time') filterTime = val;
      else if (filter === 'workers') filterWorkers = !filterWorkers;
      saveFilters();
      applyFilters();
      renderFilters(window.__allSessions || []);
    });
  });
}

function matchesFilters(s, q) {
  const lq = (q || '').toLowerCase();
  if (lq) {
    const text = (s.id + ' ' + (s.cwd || '') + ' ' + (s.instance || '') + ' ' + (s.name || '') + ' ' + (s.last_prompt || '')).toLowerCase();
    if (!text.includes(lq)) return false;
  }
  if (filterStatus !== 'all' && s.status !== 'archived' && s.status !== filterStatus) return false;
  if (filterInstance !== 'all' && s.instance !== filterInstance) return false;
  if (filterTime !== 'all' && s.last_activity) {
    const now = Date.now();
    if (filterTime === '5m' && (now - s.last_activity) > 5 * 60 * 1000) return false;
    if (filterTime === '30m' && (now - s.last_activity) > 30 * 60 * 1000) return false;
    if (filterTime === '1h' && (now - s.last_activity) > 60 * 60 * 1000) return false;
    if (filterTime === 'today') {
      const d = new Date(s.last_activity);
      const now2 = new Date();
      if (d.getFullYear() !== now2.getFullYear() || d.getMonth() !== now2.getMonth() || d.getDate() !== now2.getDate()) return false;
    }
  }
  return true;
}

function applyFilters() {
  if (inSearchMode) return;
  const q = document.getElementById('search').value;
  document.querySelectorAll('.srow').forEach(function(row) {
    const id = row.dataset.id;
    const s = (window.__allSessions || []).find(function(x) { return x.id === id; });
    if (!filterWorkers && s && s.kind === 'worker') {
      row.style.display = 'none';
      return;
    }
    row.style.display = s && matchesFilters(s, q) ? '' : 'none';
  });
}

function render(data) {
  lang = data.language || "es";
  t = I18N[lang] || I18N.es;

  var resumeBtn = document.getElementById("d-resume-copy");
  if (resumeBtn) resumeBtn.textContent = t.resume_copy || "Copiar prompt";
  var copyIdBtn = document.getElementById("d-copy-id");
  if (copyIdBtn) copyIdBtn.textContent = t.resume_copy_id || "copiar id";

  document.getElementById("search").placeholder = t.search_placeholder;

  const active = data.active || [];
  const archived = data.archived || [];
  window.__allSessions = [...active, ...archived];
  window.__lastRenderData = data;

  if (inSearchMode) return;

  document.getElementById("lbl-active").textContent =
    t.sessions + " · " + active.length + " " + t.active;
  document.getElementById("lbl-archived").textContent = t.archived;

  const activeRegular = active.filter(function (s) { return s.kind !== 'worker'; });
  const activeWorkers = active.filter(function (s) { return s.kind === 'worker'; });
  const archivedRegular = archived.filter(function (s) { return s.kind !== 'worker'; });
  const archivedWorkers = archived.filter(function (s) { return s.kind === 'worker'; });

  const sessEl = document.getElementById("sessions");
  sessEl.innerHTML = (activeRegular.length
    ? activeRegular.map(buildRow).join("")
    : '<div class="row muted">' + esc(t.no_sessions) + "</div>")
    + buildWorkersGroup(activeWorkers);

  const archEl = document.getElementById("archived");
  archEl.innerHTML = archivedRegular.map(buildRow).join("")
    + buildWorkersGroup(archivedWorkers);

  attachRowHandlers();

  if (openId) {
    document.querySelectorAll('.srow[data-id="' + openId + '"]').forEach(function (r) {
      r.classList.add("sel");
    });
  }

  renderFilters(window.__allSessions);
  applyFilters();
}

function fmtDailyDate(dateStr) {
  var parts = (dateStr || '').split('-');
  if (parts.length !== 3) return dateStr || '';
  var months = {
    es: ['ene','feb','mar','abr','may','jun','jul','ago','sep','oct','nov','dic'],
    en: ['jan','feb','mar','apr','may','jun','jul','aug','sep','oct','nov','dec'],
    pt: ['jan','fev','mar','abr','mai','jun','jul','ago','set','out','nov','dez'],
  };
  var month = parseInt(parts[1], 10) - 1;
  var day = parseInt(parts[2], 10);
  var mnames = months[lang] || months.es;
  return day + ' ' + (mnames[month] || parts[1]);
}

function renderMdBullets(md) {
  if (!md || !md.trim()) return '<span class="muted">' + esc(t.daily_empty || '—') + '</span>';
  var items = md.split('\n')
    .map(function(l) { return l.trim(); })
    .filter(function(l) { return l.length > 0; })
    .map(function(l) {
      return '<li>' + esc(l.startsWith('- ') ? l.slice(2) : l) + '</li>';
    });
  if (!items.length) return '<span class="muted">' + esc(t.daily_empty || '—') + '</span>';
  return '<ul class="daily-bullets">' + items.join('') + '</ul>';
}

function getDailyLang(daily) {
  // Read persisted choice; default to app lang if es/en, else es
  var stored = localStorage.getItem('cl-daily-lang');
  if (stored === 'es' || stored === 'en') return stored;
  return (lang === 'es' || lang === 'en') ? lang : 'es';
}

function buildSlackMrkdwn(daily, lang) {
  var t2 = I18N[lang] || I18N.es;
  var sections = [
    { label: t2.daily_yesterday || 'Ayer', md: lang === 'en' ? (daily.yesterday_md_en || daily.yesterday_md) : daily.yesterday_md },
    { label: t2.daily_today || 'Hoy', md: lang === 'en' ? (daily.today_md_en || daily.today_md) : daily.today_md },
    { label: t2.daily_blockers || 'Bloqueos', md: lang === 'en' ? (daily.blockers_md_en || daily.blockers_md) : daily.blockers_md },
  ];

  function fmtSlackDate(dateStr) {
    var parts = (dateStr || '').split('-');
    if (parts.length !== 3) return dateStr || '';
    var month = parseInt(parts[1], 10) - 1;
    var day = parseInt(parts[2], 10);
    if (isNaN(month) || isNaN(day) || month < 0 || month > 11) return dateStr || '';
    var enMonths = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
    var esMonths = ['ene','feb','mar','abr','may','jun','jul','ago','sep','oct','nov','dic'];
    var ptMonths = ['jan','fev','mar','abr','mai','jun','jul','ago','set','out','nov','dez'];
    if (lang === 'en') return (enMonths[month] || '') + ' ' + day;
    var mnames = lang === 'pt' ? ptMonths : esMonths;
    return day + ' ' + (mnames[month] || '');
  }

  var parts = ['📋 Daily · ' + fmtSlackDate(daily.date), ''];
  var firstSection = true;
  for (var i = 0; i < sections.length; i++) {
    var sec = sections[i];
    var bullets = (sec.md || '').split('\n')
      .map(function(l) { return l.trim(); })
      .filter(function(l) { return l.length > 0; })
      .map(function(l) { return l.startsWith('- ') ? l.slice(2) : l; })
      .filter(function(l) { return l.length > 0; });
    if (bullets.length === 0) continue;
    if (!firstSection) parts.push('');
    firstSection = false;
    parts.push(sec.label);
    for (var j = 0; j < bullets.length; j++) {
      parts.push('• ' + bullets[j]);
    }
  }
  return parts.join('\n');
}

function renderDaily(daily) {
  var card = document.getElementById('daily-card');
  if (!card) return;
  if (!daily || !daily.date) {
    card.hidden = true;
    return;
  }

  // Determine and persist language choice
  dailyLang = getDailyLang(daily);

  var fmtDate = fmtDailyDate(daily.date);

  function getField(esField, enField) {
    if (dailyLang === 'en') {
      var v = daily[enField];
      return (v !== null && v !== undefined && v !== '') ? v : daily[esField];
    }
    return daily[esField];
  }

  var sections = [
    {
      color: '#3fb950',
      icon: '✓',
      label: t.daily_yesterday || 'Ayer',
      desc: t.day_yesterday_desc || 'lo que avancé',
      md: getField('yesterday_md', 'yesterday_md_en'),
    },
    {
      color: '#58a6ff',
      icon: '→',
      label: t.daily_today || 'Hoy',
      desc: t.day_today_desc || 'lo que sigue',
      md: getField('today_md', 'today_md_en'),
    },
    {
      color: '#d29922',
      icon: '⚠',
      label: t.daily_blockers || 'Bloqueos',
      desc: t.day_blockers_desc || 'lo que me traba',
      md: getField('blockers_md', 'blockers_md_en'),
    },
  ];

  var body = sections.map(function(sec) {
    return '<div class="daily-section" style="border-left:2px solid ' + sec.color + ';padding-left:10px;margin:8px 0;">' +
      '<div class="daily-sublbl">' +
        '<span style="color:' + sec.color + '">' + sec.icon + '</span>' +
        '<span style="color:' + sec.color + ';font-size:10px;letter-spacing:.10em;text-transform:uppercase;">' + esc(sec.label) + '</span>' +
        '<span class="daily-lbl-desc">' + esc(sec.desc) + '</span>' +
      '</div>' +
      renderMdBullets(sec.md) +
    '</div>';
  }).join('');

  var activeLangEs = dailyLang === 'es' ? ' style="font-weight:bold;text-decoration:underline"' : '';
  var activeLangEn = dailyLang === 'en' ? ' style="font-weight:bold;text-decoration:underline"' : '';

  card.innerHTML =
    '<details class="dsec" open>' +
      '<summary>' +
        '<span class="arr">▶</span>' +
        '<span class="lbl">📋 <strong>DAILY</strong> · ' + esc(fmtDate) + ' <span class="daily-auto-pill">· ' + esc(t.daily_auto || 'auto') + '</span></span>' +
        '<span class="daily-lang-toggle">' +
          '<button class="daily-lang-btn" data-lang="es"' + activeLangEs + '>' + esc(t.daily_lang_es || 'ES') + '</button>' +
          '<button class="daily-lang-btn" data-lang="en"' + activeLangEn + '>' + esc(t.daily_lang_en || 'EN') + '</button>' +
        '</span>' +
        '<button class="daily-slack-copy" title="' + esc(t.daily_copy_slack || 'copy for Slack') + '">📋 Slack</button>' +
        '<button class="daily-regen" title="' + esc(t.daily_regenerate || 'Regenerar') + '">↻</button>' +
      '</summary>' +
      '<div class="body">' + body + '</div>' +
    '</details>';

  card.hidden = false;

  // Lang toggle buttons
  card.querySelectorAll('.daily-lang-btn').forEach(function(btn) {
    btn.addEventListener('click', function(e) {
      e.stopPropagation();
      var chosen = btn.getAttribute('data-lang');
      localStorage.setItem('cl-daily-lang', chosen);
      dailyLang = chosen;
      renderDaily(daily);
    });
  });

  // Copy for Slack button
  card.querySelector('.daily-slack-copy').addEventListener('click', function(e) {
    e.stopPropagation();
    var mrkdwn = buildSlackMrkdwn(daily, dailyLang);
    navigator.clipboard.writeText(mrkdwn)
      .then(function() { showToast(t.resume_copied || 'copiado'); })
      .catch(function() { showToast(t.resume_failed || 'error al copiar'); });
  });

  // Regen button
  card.querySelector('.daily-regen').addEventListener('click', function(e) {
    e.stopPropagation();
    fetch('/api/daily/regenerate?force=1', { method: 'POST' })
      .then(function(res) { return res.ok ? res.json() : null; })
      .then(function(data) { if (data && data.date) renderDaily(data); })
      .catch(function() {});
  });
}

function renderSlack(inbox) {
  var mentions = (inbox && inbox.mentions) || [];
  var signals = (inbox && inbox.signals) || [];

  var slackCard = document.getElementById('slack-card');
  if (slackCard) {
    if (mentions.length === 0) {
      slackCard.hidden = true;
    } else {
      var mentionsHtml = mentions.map(function(m) {
        var excerpt = esc((m.text || '').slice(0, 100));
        var reaskHtml = '';
        if (m.ask_count > 1) {
          reaskHtml = ' <span class="badge-sys">' + m.ask_count + ' ' + esc(t.slack_reasks || 're-asks') + '</span>';
          if (m.last_at) reaskHtml += ' <span class="dim">' + rel(m.last_at) + '</span>';
        }
        var sessionHtml = (m.session_id && m.session_name)
          ? ' <span class="slack-session" data-sid="' + esc(m.session_id) + '" style="cursor:pointer;text-decoration:underline">' + esc(m.session_name) + '</span>'
          : ' <span class="dim">' + esc(t.slack_unlinked || 'sin ligar') + '</span>';
        return '<div class="frow slack-mention" data-mid="' + m.id + '">' +
          '<span class="amber">' + esc(m.author || '') + '</span>' +
          '<span style="flex:1;padding:0 6px;overflow:hidden;text-overflow:ellipsis">' + excerpt + '</span>' +
          reaskHtml + sessionHtml +
          '<button class="slack-resolve" data-mid="' + m.id + '">' + esc(t.slack_mark_resolved || '✓') + '</button>' +
        '</div>';
      }).join('');
      var sectionLbl = esc(t.slack_section || 'Slack') + ' · ' + mentions.length + ' ' + esc(t.slack_unresolved || 'sin resolver');
      slackCard.innerHTML =
        '<details class="dsec" open>' +
          '<summary><span class="arr">▶</span><span class="lbl">' + sectionLbl + '</span></summary>' +
          '<div class="body">' + mentionsHtml + '</div>' +
        '</details>';
      slackCard.hidden = false;

      slackCard.querySelectorAll('.slack-resolve').forEach(function(btn) {
        btn.addEventListener('click', function(e) {
          e.stopPropagation();
          var mid = btn.dataset.mid;
          fetch('/api/mentions/' + mid + '/resolve', { method: 'POST' })
            .then(function(res) { return res.ok ? res.json() : null; })
            .then(function(data) {
              if (!data) return;
              var row = slackCard.querySelector('.slack-mention[data-mid="' + mid + '"]');
              if (row) row.remove();
              var remaining = slackCard.querySelectorAll('.slack-mention').length;
              if (remaining === 0) {
                slackCard.hidden = true;
              } else {
                var lbl = slackCard.querySelector('.lbl');
                if (lbl) lbl.innerHTML = esc(t.slack_section || 'Slack') + ' · ' + remaining + ' ' + esc(t.slack_unresolved || 'sin resolver');
              }
            })
            .catch(function() {});
        });
      });
      slackCard.querySelectorAll('.slack-session').forEach(function(el) {
        el.addEventListener('click', function() { openDetail(el.dataset.sid); });
      });
    }
  }

  var signalsCard = document.getElementById('signals-card');
  if (signalsCard) {
    if (signals.length === 0) {
      signalsCard.hidden = true;
    } else {
      var signalsHtml = signals.map(function(sg) {
        var icon = sg.kind === 'deploy' ? '🚀' : '⚠';
        var kindLabel = sg.kind === 'deploy' ? esc(t.signal_deploy || 'deploy') : esc(t.signal_alert || 'alerta');
        var excerpt = esc((sg.text || '').slice(0, 100));
        var sessionHtml = (sg.session_id && sg.session_name)
          ? ' <span class="slack-session" data-sid="' + esc(sg.session_id) + '" style="cursor:pointer;text-decoration:underline">' + esc(sg.session_name) + '</span>'
          : ' <span class="dim">' + esc(t.slack_unlinked || 'sin ligar') + '</span>';
        return '<div class="frow">' +
          '<span class="badge-sys">' + icon + ' ' + kindLabel + '</span>' +
          '<span style="flex:1;padding:0 6px;overflow:hidden;text-overflow:ellipsis">' + excerpt + '</span>' +
          sessionHtml +
        '</div>';
      }).join('');
      signalsCard.innerHTML =
        '<details class="dsec">' +
          '<summary><span class="arr">▶</span><span class="lbl">' + esc(t.signals_section || 'Señales') + ' · ' + signals.length + '</span></summary>' +
          '<div class="body">' + signalsHtml + '</div>' +
        '</details>';
      signalsCard.hidden = false;
      signalsCard.querySelectorAll('.slack-session').forEach(function(el) {
        el.addEventListener('click', function() { openDetail(el.dataset.sid); });
      });
    }
  }
}

function buildSection(label, hint, bodyHtml, open) {
  return (
    "<details class=\"dsec\"" + (open ? " open" : "") + ">" +
      "<summary>" +
        '<span class="arr">▶</span>' +
        '<span class="lbl">' + esc(label) + "</span>" +
        '<span class="hint">' + esc(hint) + "</span>" +
      "</summary>" +
      '<div class="body">' + bodyHtml + "</div>" +
    "</details>"
  );
}

function openDetail(id) {
  if (!id) return;
  openId = id;

  document.querySelectorAll(".srow").forEach(function (r) { r.classList.remove("sel"); });
  document.querySelectorAll('.srow[data-id="' + id + '"]').forEach(function (r) {
    r.classList.add("sel");
  });

  var detail = document.getElementById("detail");
  detail.removeAttribute("hidden");

  fetch("/api/sessions/" + id)
    .then(function (res) { return res.ok ? res.json() : null; })
    .then(function (data) {
      if (!data) return;
      var session = data.session;
      var files = data.files || [];
      var events = data.events || [];
      var tasks = data.tasks || [];
      var links = data.links || [];
      var prLinks = links.filter(function(l) { return l.kind === 'pr'; });
      var linearLinks = links.filter(function(l) { return l.kind === 'linear'; });
      var artifactLinks = links.filter(function(l) { return l.kind === 'artifact'; });

      var displayName = session.name || basename(session.cwd) || session.id;
      document.querySelector("#detail .d-title").textContent = displayName;

      var dur = session.started_at ? fmtDur(Date.now() - session.started_at) : "";
      var startedStr = fmtTime(session.started_at);

      var statusLine = "";
      if (session.status === "waiting_input") {
        statusLine = '<div class="amber">● ' + esc(t.waiting_you) + " · " + esc(dur) + " · " + esc(t.started) + " " + esc(startedStr) + "</div>";
      } else if (session.status === "running") {
        statusLine = '<div class="green">● ' + esc(t.running) + " · " + esc(dur) + " · " + esc(t.started) + " " + esc(startedStr) + "</div>";
      } else if (session.status === "idle") {
        statusLine = '<div class="muted">● ' + esc(t.idle) + " · " + esc(dur) + " · " + esc(t.started) + " " + esc(startedStr) + "</div>";
      } else {
        var endedStr = fmtTime(session.ended_at);
        var totalDur = session.ended_at && session.started_at ? fmtDur(session.ended_at - session.started_at) : "";
        statusLine = '<div class="dim">● ' + esc(t.ended) + " " + esc(endedStr) + " · " + esc(archiveReasonLabel(session.archived_reason)) + " · " + esc(totalDur) + "</div>";
      }

      var resumen = "";
      if (session.summary) {
        resumen = '<div class="muted" style="margin:10px 0;line-height:1.7">' + esc(session.summary) + '</div>';
        if (session.summary_next) {
          resumen += '<div class="amber" style="margin:4px 0;line-height:1.7">→ ' + esc(session.summary_next) + '</div>';
        }
      } else if (session.last_prompt) {
        var rdec = decodePrompt(session.last_prompt);
        resumen = '<div class="muted" style="margin:10px 0;line-height:1.7">' +
          (rdec.kind === "system"
            ? sysChip(rdec.label, rdec.text)
            : esc(session.last_prompt)) +
        "</div>";
      }
      if (!session.summary) {
        resumen += '<button class="sbtn-clear" id="d-gen-summary" data-id="' + esc(session.id) + '" style="margin:4px 0">' +
          esc(t.summary_generate || 'generar resumen') + '</button>';
      }

      var metaLine =
        '<div class="detail-meta">' +
          '<span class="badge-instance tag">' + esc(session.instance || "") + "</span>" +
          ' <span class="muted">' + esc(session.cwd || "") + "</span>" +
        "</div>";

      var filesHint = files.length ? files.length + " " + t.files : "0";
      var session_cwd = session.cwd || '';
      var groups = new Map();
      files.forEach(function(f) {
        var g = fileGroup(f.path, session_cwd);
        if (!groups.has(g.dir)) groups.set(g.dir, { label: g.label, files: [] });
        groups.get(g.dir).files.push(f);
      });
      var groupCount = groups.size;
      var filesBody = '';
      if (files.length === 0) {
        filesBody = '<div class="dim">—</div>';
      } else {
        groups.forEach(function(grp) {
          var openAttr = groupCount <= 3 ? ' open' : '';
          filesBody += '<details class="fgroup"' + openAttr + '>';
          filesBody += '<summary class="fgroup-summary">';
          filesBody += '<span class="fgroup-dir">' + esc(grp.label) + '</span>';
          filesBody += ' <span class="dim">(' + grp.files.length + ')</span>';
          filesBody += '</summary>';
          grp.files.forEach(function(f) {
            var bn = basename(f.path);
            filesBody +=
              '<div class="frow file-row" data-path="' + esc(f.path) + '" data-sid="' + esc(session.id) + '" style="cursor:pointer">' +
                '<span class="file-bn">' + esc(bn) + '</span>' +
                '<span class="badge-sys file-action">' + esc(f.change_kind) + '</span>' +
                '<span class="dim">' + rel(f.ts) + '</span>' +
              '</div>' +
              '<div class="file-preview" data-path="' + esc(f.path) + '" hidden title="' + esc(f.path) + '"></div>';
          });
          filesBody += '</details>';
        });
      }

      var lastEvent = events[0];
      var eventsHint = lastEvent ? fmtTime(lastEvent.ts) : "";

      var presentEvKinds = [];
      events.forEach(function(e) {
        if (presentEvKinds.indexOf(e.kind) === -1) presentEvKinds.push(e.kind);
      });

      var savedEvFilter = [];
      try {
        var storedEv = localStorage.getItem('cl-events-filter');
        if (storedEv) savedEvFilter = JSON.parse(storedEv);
      } catch {}
      var activeEvKinds = new Set(Array.isArray(savedEvFilter) ? savedEvFilter : []);

      var eventsRows = events.length
        ? events.map(function(e) {
            var meta = eventMeta(e.kind);
            var detailHtml;
            if (e.kind === "prompt" && e.detail) {
              var edec = decodePrompt(e.detail);
              detailHtml = edec.kind === "system"
                ? '<span style="flex:1;min-width:0;overflow:hidden;text-overflow:ellipsis">' + sysChip(edec.label, edec.text) + "</span>"
                : '<span class="muted" style="flex:1;min-width:0;overflow:hidden;text-overflow:ellipsis">' + esc(e.detail) + "</span>";
            } else {
              detailHtml = '<span class="muted" style="flex:1;min-width:0;overflow:hidden;text-overflow:ellipsis">' + esc(e.detail || "") + "</span>";
            }
            return (
              '<div class="frow" data-kind="' + esc(e.kind) + '">' +
                '<span class="dim">' + esc(fmtTime(e.ts)) + "</span>" +
                '<span class="ev-kind" style="color:' + meta.color + '">' + meta.icon + ' ' + esc(e.kind) + "</span>" +
                detailHtml +
              "</div>"
            );
          }).join("")
        : '';

      var eventsBody = events.length
        ? buildEventsFilterRow(presentEvKinds, activeEvKinds) + eventsRows
        : '<div class="dim">—</div>';

      var tasksBody = buildTasksBody(tasks);
      var tasksHint = tasks.filter(function(t2) { return t2.status !== 'done'; }).length + ' ' + (t.task_open || 'open');

      var slackMentions = data.mentions || [];
      var slackSection = '';
      if (slackMentions.length > 0) {
        var smBody = slackMentions.map(function(m) {
          var excerpt = esc((m.text || '').slice(0, 120));
          var parts = [];
          try { parts = JSON.parse(m.participants || '[]'); } catch {}
          var reask = m.ask_count > 1 ? ' <span class="badge-sys">×' + m.ask_count + '</span>' : '';
          var age = m.last_at ? ' <span class="dim">' + rel(m.last_at) + '</span>' : '';
          var pHtml = parts.length ? ' <span class="dim">+' + parts.length + '</span>' : '';
          return '<div class="frow">' +
            '<span class="amber">' + esc(m.author || '') + '</span>' +
            '<span style="flex:1;padding:0 6px;overflow:hidden;text-overflow:ellipsis">' + excerpt + '</span>' +
            reask + age + pHtml +
          '</div>';
        }).join('');
        slackSection = buildSection('Slack ligado', slackMentions.length + '', smBody, true);
      }

      var prSection = '';
      if (prLinks.length) {
        var prBody = prLinks.map(function(link) {
          var rm = link.ref.match(/^(.+)#(\d+)$/);
          var repoName = rm ? rm[1] : link.ref;
          var prNum = rm ? rm[2] : '';
          var meta = {};
          try { meta = JSON.parse(link.meta || '{}'); } catch (e) {}
          var stateBadge = '';
          if (meta.state === 'OPEN') {
            stateBadge = '<span class="amber">' + esc(meta.state) + '</span> ';
          } else if (meta.state === 'MERGED' || meta.state === 'CLOSED') {
            stateBadge = '<span class="dim">' + esc(meta.state) + '</span> ';
          }
          var checksBadge = '';
          if (Array.isArray(meta.checks) && meta.checks.length > 0) {
            var hasFailure = meta.checks.some(function(c) { return c.conclusion === 'FAILURE'; });
            checksBadge = hasFailure
              ? ' <span class="red">checks fail</span>'
              : ' <span class="green">checks ok</span>';
          }
          var label = esc(link.title || link.ref);
          var fullRepo = meta && meta.fullRepo ? meta.fullRepo : null;
          var href = fullRepo ? 'https://github.com/' + esc(fullRepo) + '/pull/' + esc(prNum) : null;
          var linkContent = href
            ? '<a href="' + href + '" target="_blank" rel="noopener">' + label + '</a>'
            : label;
          return '<div class="frow">' +
            stateBadge + checksBadge +
            '<span style="flex:1;padding:0 6px;overflow:hidden;text-overflow:ellipsis">' +
              linkContent +
            '</span>' +
          '</div>';
        }).join('');
        prSection = buildSection(t.links_repos || 'Repos & PRs', prLinks.length + '', prBody, false);
      }

      var linearSection = '';
      if (linearLinks.length) {
        var linBody = linearLinks.map(function(link) {
          var label = link.title
            ? esc(link.ref) + ' — ' + esc(link.title)
            : esc(link.ref);
          return '<div class="frow">' +
            '<span style="flex:1;padding:0 6px;overflow:hidden;text-overflow:ellipsis">' +
              '<a href="https://linear.app/issue/' + esc(link.ref) + '" target="_blank" rel="noopener">' + label + '</a>' +
            '</span>' +
          '</div>';
        }).join('');
        linearSection = buildSection(t.links_linear || 'Linear', linearLinks.length + '', linBody, false);
      }

      var artifactSection = '';
      if (artifactLinks.length) {
        var artBody = artifactLinks.map(function(link) {
          var path = link.ref;
          return '<div class="frow file-row" data-path="' + esc(path) + '" data-sid="' + esc(session.id) + '" style="cursor:pointer">' +
              '<span class="file-bn">' + esc(basename(path)) + '</span>' +
              '<span class="dim">' + esc(path) + '</span>' +
            '</div>' +
            '<div class="file-preview" data-path="' + esc(path) + '" hidden title="' + esc(path) + '"></div>';
        }).join('');
        artifactSection = buildSection(t.links_artifacts || 'Artifacts', artifactLinks.length + '', artBody, false);
      }

      document.getElementById("d-body").innerHTML =
        statusLine +
        resumen +
        metaLine +
        (tasks.length ? buildSection(t.tasks_section || 'Tareas', tasksHint, tasksBody, true) : '') +
        slackSection +
        (prLinks.length ? prSection : '') +
        (linearLinks.length ? linearSection : '') +
        (artifactLinks.length ? artifactSection : '') +
        buildSection(t.files, filesHint, filesBody, false) +
        buildSection(t.events, eventsHint, eventsBody, true);

      var genBtn = document.getElementById('d-gen-summary');
      if (genBtn) {
        genBtn.addEventListener('click', function(e) {
          e.stopPropagation();
          genBtn.disabled = true;
          genBtn.textContent = '…';
          fetch('/api/sessions/' + encodeURIComponent(genBtn.dataset.id) + '/summarize', { method: 'POST' })
            .then(function(res) { return res.ok ? res.json() : Promise.reject(res.status); })
            .then(function() { openDetail(genBtn.dataset.id); })
            .catch(function() { genBtn.disabled = false; genBtn.textContent = t.summary_generate || 'generar resumen'; });
        });
      }

      document.querySelectorAll('#d-body .file-row').forEach(function(row) {
        row.addEventListener('click', function(e) {
          e.stopPropagation();
          var path = row.dataset.path;
          var sid = row.dataset.sid;
          var previewEl = row.nextElementSibling;
          if (!previewEl || !previewEl.classList.contains('file-preview')) return;

          if (!previewEl.hidden) {
            previewEl.hidden = true;
            return;
          }

          previewEl.textContent = '…';
          previewEl.hidden = false;

          var url = '/api/sessions/' + encodeURIComponent(sid) + '/file?path=' + encodeURIComponent(path);
          fetch(url)
            .then(function(res) {
              if (!res.ok) {
                return res.json().then(function(data) {
                  var key = data.error === 'too_large' ? 'preview_too_large'
                          : data.error === 'missing' ? 'preview_missing'
                          : data.error === 'binary' ? 'preview_binary'
                          : null;
                  var msg = key ? (t[key] || data.error) : String(data.error || '');
                  previewEl.innerHTML = '<span class="dim">' + esc(msg) + '</span>';
                });
              }
              var ct = res.headers.get('content-type') || '';
              if (ct.startsWith('image/')) {
                var img = document.createElement('img');
                img.src = url;
                img.className = 'file-img';
                previewEl.innerHTML = '';
                previewEl.appendChild(img);
                return;
              }
              return res.text().then(function(text) {
                previewEl.innerHTML = '';
                if (path.toLowerCase().endsWith('.md')) {
                  var div = document.createElement('div');
                  div.className = 'md-preview';
                  div.innerHTML = mdToSafeHtml(esc(text));
                  previewEl.appendChild(div);
                } else {
                  var pre = document.createElement('pre');
                  pre.className = 'file-pre';
                  pre.textContent = text;
                  previewEl.appendChild(pre);
                }
              });
            })
            .catch(function() {
              previewEl.innerHTML = '<span class="dim">' + esc(t.preview_error || 'error') + '</span>';
            });
        });
      });

      // Task accordion click handlers
      document.querySelectorAll('#d-body .task-row').forEach(function(taskRow) {
        taskRow.addEventListener('click', function(e) {
          e.stopPropagation();
          var idx = taskRow.dataset.taskIdx;
          var expanded = document.querySelector('#d-body .task-expanded[data-task-idx="' + idx + '"]');
          if (!expanded) return;
          expanded.hidden = !expanded.hidden;
          var caret = taskRow.querySelector('.task-caret');
          if (caret) caret.textContent = expanded.hidden ? '▶' : '▼';
        });
      });

      // Event filter handlers
      (function() {
        var filterBar = document.querySelector('#d-body .ev-filter-row');
        if (!filterBar) return;

        function applyEvFilter() {
          var isAll = activeEvKinds.size === 0;
          document.querySelectorAll('#d-body .frow[data-kind]').forEach(function(row) {
            row.style.display = isAll || activeEvKinds.has(row.dataset.kind) ? '' : 'none';
          });
          filterBar.querySelectorAll('.ev-chip').forEach(function(chip) {
            var k = chip.dataset.kind;
            var on;
            if (k === 'all') on = isAll;
            else if (k === '__solo_prompts__') on = activeEvKinds.size === 1 && activeEvKinds.has('prompt');
            else on = isAll || activeEvKinds.has(k);
            chip.classList.toggle('active', on);
          });
        }

        applyEvFilter();

        filterBar.querySelectorAll('.ev-chip').forEach(function(chip) {
          chip.addEventListener('click', function() {
            var k = chip.dataset.kind;
            if (k === 'all') {
              activeEvKinds = new Set();
            } else if (k === '__solo_prompts__') {
              activeEvKinds = (activeEvKinds.size === 1 && activeEvKinds.has('prompt'))
                ? new Set()
                : new Set(['prompt']);
            } else {
              if (activeEvKinds.has(k)) {
                activeEvKinds.delete(k);
              } else {
                activeEvKinds.add(k);
                if (presentEvKinds.every(function(pk) { return activeEvKinds.has(pk); })) {
                  activeEvKinds = new Set();
                }
              }
            }
            if (activeEvKinds.size === 0) {
              localStorage.removeItem('cl-events-filter');
            } else {
              localStorage.setItem('cl-events-filter', JSON.stringify(Array.from(activeEvKinds)));
            }
            applyEvFilter();
          });
        });
      })();
    });
}

function openSettings() {
  var panel = document.getElementById('settings-panel');
  var title = document.getElementById('settings-title');
  var body = document.getElementById('settings-body');
  if (title) title.textContent = t.settings_title || 'Settings';
  if (body) body.innerHTML = '<div class="dim">…</div>';
  panel.removeAttribute('hidden');

  fetch('/api/config')
    .then(function(res) { return res.ok ? res.json() : Promise.reject(res.status); })
    .then(function(cfg) {
      window.__settingsConfig = cfg;

      var langOpts = ['es', 'en', 'pt'].map(function(l) {
        return '<option value="' + esc(l) + '"' + (cfg.language === l ? ' selected' : '') + '>' + esc(l) + '</option>';
      }).join('');

      function chk(id, key, val) {
        return '<label class="scheck"><input type="checkbox" id="' + esc(id) + '"' + (val ? ' checked' : '') + '><span>' + esc(t[key] || key) + '</span></label>';
      }

      var instHtml = '';
      if (cfg.detectedInstances && cfg.detectedInstances.length > 0) {
        var enabledDirs = new Set((cfg.instances || []).map(function(i) { return i.dir; }));
        instHtml = '<div class="sinst">' +
          cfg.detectedInstances.map(function(di) {
            var checked = enabledDirs.has(di.dir);
            return '<label class="scheck">' +
              '<input type="checkbox" class="inst-chk" data-dir="' + esc(di.dir) + '" data-name="' + esc(di.name) + '"' + (checked ? ' checked' : '') + '>' +
              '<span>' + esc(di.name) + ' <span class="dim">' + esc(di.dir) + '</span></span>' +
            '</label>';
          }).join('') +
        '</div>';
      }

      var tokPlaceholder = cfg.slackTokenSet ? (t.settings_slack_token_set || 'set ····') + cfg.slackTokenLast4 : '';
      var linPlaceholder = cfg.linearTokenSet ? (t.settings_slack_token_set || 'set ····') + (cfg.linearTokenLast4 || '') : '';

      body.innerHTML =
        '<div class="sfield">' +
          '<label>' + esc(t.settings_language || 'Language') + '</label>' +
          '<select id="s-lang">' + langOpts + '</select>' +
        '</div>' +
        chk('s-notify', 'settings_notify', cfg.notifyWaiting) +
        chk('s-summaries', 'settings_summaries_auto', cfg.summariesAuto) +
        chk('s-daily', 'settings_daily_auto', cfg.dailyAuto) +
        '<div class="sfield">' +
          '<label>' + esc(t.settings_alerts || 'Alert channels') + '</label>' +
          '<input type="text" id="s-alerts" value="' + esc((cfg.slackChannelsAlerts || []).join(', ')) + '">' +
        '</div>' +
        '<div class="sfield">' +
          '<label>' + esc(t.settings_deploys || 'Deploy channels') + '</label>' +
          '<input type="text" id="s-deploys" value="' + esc((cfg.slackChannelsDeploys || []).join(', ')) + '">' +
        '</div>' +
        (instHtml ? '<div class="sfield"><label>' + esc(t.settings_instances || 'Instances') + '</label>' + instHtml + '</div>' : '') +
        '<div class="sfield">' +
          '<label>' + esc(t.settings_slack_token || 'Slack token') + '</label>' +
          '<div class="stoken-row">' +
            '<input type="password" id="s-token" placeholder="' + esc(tokPlaceholder) + '" value="">' +
            '<button class="sbtn-clear" id="s-token-clear">' + esc(t.settings_clear || 'Clear') + '</button>' +
          '</div>' +
        '</div>' +
        '<div class="sfield">' +
          '<label>' + esc(t.settings_linear_token || 'Linear token') + '</label>' +
          '<div class="stoken-row">' +
            '<input type="password" id="s-lintoken" placeholder="' + esc(linPlaceholder) + '" value="">' +
            '<button class="sbtn-clear" id="s-lintoken-clear">' + esc(t.settings_clear || 'Clear') + '</button>' +
          '</div>' +
        '</div>' +
        '<button class="sbtn-save" id="s-save">' + esc(t.settings_save || 'Save') + '</button>';

      document.getElementById('s-token-clear').addEventListener('click', function() {
        document.getElementById('s-token').value = '';
        document.getElementById('s-token').placeholder = '';
        window.__settingsTokenCleared = true;
      });
      document.getElementById('s-lintoken-clear').addEventListener('click', function() {
        document.getElementById('s-lintoken').value = '';
        document.getElementById('s-lintoken').placeholder = '';
        window.__settingsLinTokenCleared = true;
      });

      document.getElementById('s-save').addEventListener('click', saveSettings);
    })
    .catch(function() {
      if (body) body.innerHTML = '<div class="dim">error</div>';
    });
}

function saveSettings() {
  var cfg = window.__settingsConfig || {};
  var payload = {};

  var langEl = document.getElementById('s-lang');
  if (langEl) payload.language = langEl.value;

  var notifyEl = document.getElementById('s-notify');
  if (notifyEl) payload.notifyWaiting = notifyEl.checked;

  var summEl = document.getElementById('s-summaries');
  if (summEl) payload.summariesAuto = summEl.checked;

  var dailyEl = document.getElementById('s-daily');
  if (dailyEl) payload.dailyAuto = dailyEl.checked;

  var alertsEl = document.getElementById('s-alerts');
  if (alertsEl) {
    payload.slackChannelsAlerts = alertsEl.value.split(',').map(function(s) { return s.trim(); }).filter(Boolean);
  }

  var deploysEl = document.getElementById('s-deploys');
  if (deploysEl) {
    payload.slackChannelsDeploys = deploysEl.value.split(',').map(function(s) { return s.trim(); }).filter(Boolean);
  }

  var instChecks = document.querySelectorAll('.inst-chk:checked');
  var instances = [];
  instChecks.forEach(function(cb) {
    instances.push({ dir: cb.dataset.dir, name: cb.dataset.name });
  });
  payload.instances = instances;

  var tokenEl = document.getElementById('s-token');
  if (tokenEl) {
    var tokVal = tokenEl.value;
    var tokPlaceholder = cfg.slackTokenSet ? (t.settings_slack_token_set || 'set ····') + (cfg.slackTokenLast4 || '') : '';
    if (window.__settingsTokenCleared) {
      payload.slackToken = '';
    } else if (tokVal && tokVal !== tokPlaceholder) {
      payload.slackToken = tokVal;
    }
    // if tokVal is empty and not cleared, and token was set: omit → server keeps token
  }

  var linEl = document.getElementById('s-lintoken');
  if (linEl) {
    var linVal = linEl.value;
    var linPlaceholder = cfg.linearTokenSet ? (t.settings_slack_token_set || 'set ····') + (cfg.linearTokenLast4 || '') : '';
    if (window.__settingsLinTokenCleared) {
      payload.linearToken = '';
    } else if (linVal && linVal !== linPlaceholder) {
      payload.linearToken = linVal;
    }
  }

  fetch('/api/config', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(payload),
  })
    .then(function(res) { return res.ok ? res.json() : Promise.reject(res.status); })
    .then(function() {
      closeSettings();
      showToast(t.settings_saved || 'saved');
      if (payload.language && payload.language !== lang) {
        poll();
      }
    })
    .catch(function(err) {
      showToast('error: ' + err);
    });
}

function closeSettings() {
  var panel = document.getElementById('settings-panel');
  if (panel) panel.setAttribute('hidden', '');
  window.__settingsTokenCleared = false;
  window.__settingsLinTokenCleared = false;
}

function showToast(msg) {
  var toast = document.getElementById('s-toast');
  if (!toast) {
    toast = document.createElement('div');
    toast.id = 's-toast';
    toast.className = 'stoast';
    document.body.appendChild(toast);
  }
  toast.textContent = msg;
  toast.classList.add('show');
  setTimeout(function() { toast.classList.remove('show'); }, 2000);
}

function closeDetail() {
  openId = null;
  document.getElementById("detail").setAttribute("hidden", "");
  document.querySelectorAll(".srow").forEach(function (r) { r.classList.remove("sel"); });
}

async function poll() {
  try {
    var [sessRes, dailyRes, inboxRes] = await Promise.all([
      fetch("/api/sessions"),
      fetch("/api/daily"),
      fetch("/api/inbox"),
    ]);
    if (sessRes.ok) render(await sessRes.json());
    if (dailyRes.ok) renderDaily(await dailyRes.json());
    if (inboxRes.ok) renderSlack(await inboxRes.json());
  } catch (_) {}
}

document.addEventListener("DOMContentLoaded", function () {
  loadFilters();
  document.getElementById("d-close").addEventListener("click", closeDetail);
  document.getElementById("settings-btn").addEventListener("click", openSettings);
  document.getElementById("settings-close").addEventListener("click", closeSettings);
  document.getElementById("settings-overlay").addEventListener("click", closeSettings);
  document.getElementById("d-resume-copy").addEventListener("click", function() {
    var sid = openId;
    if (!sid) return;
    var btn = document.getElementById("d-resume-copy");
    var origText = btn ? btn.textContent : "";
    if (btn) { btn.textContent = "…"; btn.disabled = true; }
    fetch("/api/sessions/" + sid + "/resume-prompt")
      .then(function(res) { return res.ok ? res.text() : Promise.reject(res.status); })
      .then(function(text) {
        if (btn) { btn.textContent = origText; btn.disabled = false; }
        if (navigator.clipboard && navigator.clipboard.writeText) {
          return navigator.clipboard.writeText(text).then(function() {
            if (btn) {
              btn.textContent = t.resume_copied || "copiado";
              setTimeout(function() { btn.textContent = origText; }, 1500);
            }
          });
        } else {
          // Clipboard API not available — show inline textarea
          var ta = document.getElementById("d-resume-textarea");
          if (!ta) {
            ta = document.createElement("textarea");
            ta.id = "d-resume-textarea";
            ta.style.cssText = "width:100%;height:120px;margin-top:8px;font-size:12px;";
            var detail = document.getElementById("detail");
            if (detail) detail.appendChild(ta);
          }
          ta.value = text;
          ta.hidden = false;
          ta.select();
        }
      })
      .catch(function() {
        if (btn) { btn.textContent = t.resume_failed || "error"; btn.disabled = false; }
      });
  });
  document.getElementById("d-copy-id").addEventListener("click", function() {
    var sid = openId;
    if (!sid) return;
    var btn = document.getElementById("d-copy-id");
    if (navigator.clipboard && navigator.clipboard.writeText) {
      navigator.clipboard.writeText(sid).then(function() {
        if (btn) {
          var orig = btn.textContent;
          btn.textContent = t.resume_copied || "copiado";
          setTimeout(function() { btn.textContent = orig; }, 1500);
        }
      }).catch(function() {
        if (btn) {
          var orig2 = btn.textContent;
          btn.textContent = t.resume_failed || "error";
          setTimeout(function() { btn.textContent = orig2; }, 1500);
        }
      });
    }
  });
  document.getElementById("search").addEventListener("input", function () {
    var q = this.value;
    if (searchDebounce) clearTimeout(searchDebounce);
    if (q.length < 2) {
      if (inSearchMode) {
        exitSearchMode();
        // restore normal render from cached data
        if (window.__lastRenderData) render(window.__lastRenderData);
      }
      applyFilters();
      return;
    }
    searchDebounce = setTimeout(function() {
      searchDebounce = null;
      fetch('/api/search?q=' + encodeURIComponent(q))
        .then(function(res) { return res.ok ? res.json() : { results: [] }; })
        .then(function(data) { renderSearchResults(data.results || []); })
        .catch(function() {});
    }, 300);
  });
  document.addEventListener("keydown", function (e) {
    if (e.key === "Escape") {
      var panel = document.getElementById('settings-panel');
      if (panel && !panel.hidden) { closeSettings(); return; }
      closeDetail();
    }
  });
  poll();
  setInterval(poll, 5000);

  // Hot reload via SSE (dev mode)
  if (typeof EventSource !== 'undefined') {
    var evtSrc = new EventSource('/api/dev-events');
    evtSrc.addEventListener('reload', function() {
      location.reload();
    });
  }
});
